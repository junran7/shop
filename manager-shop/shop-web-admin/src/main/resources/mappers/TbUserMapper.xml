<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seven.shop.admin.dao.TbUserDao">
    <resultMap id="TbUser" type="com.seven.shop.domain.entity.TbUser" >
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" property="id" jdbcType="BIGINT" />
        <id column="username" property="username" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="phone" property="phone" jdbcType="DECIMAL" />
        <result column="email" property="email" jdbcType="DECIMAL" />
        <result column="created" property="created" jdbcType="TIMESTAMP" />
        <result column="updated" property="updated" jdbcType="TIMESTAMP" />
    </resultMap>
    <sql id="tbUserColumns">
          a.id,
          a.username,
          a.password,
          a.phone,
          a.email,
          a.created,
          a.updated
    </sql>

    <select id="getById" resultMap="TbUser">
        SELECT
        <include refid="tbUserColumns" />
        FROM
        tb_user AS a
        WHERE
        a.user_id = #{id}
    </select>

    <select id="getByEmail" resultMap="TbUser">
        SELECT
        <include refid="tbUserColumns" />
        FROM
        tb_user AS a
        WHERE
        a.email = #{email}
    </select>

    <select id="selectAll" resultMap="TbUser">
        SELECT
        <include refid="tbUserColumns" />
        FROM
        tb_user AS a
        ORDER BY a.updated DESC
    </select>

    <select id="page" parameterType="map" resultMap="TbUser">
        SELECT
        <include refid="tbUserColumns" />
        FROM tb_user AS a
        <where>
            <if test="pageParams.username != null and pageParams.username != ''" >
                AND a.username LIKE CONCAT ('%', #{tb.username}, '%')
            </if>
            <if test="pageParams.phone != null and pageParams.phone != ''" >
                AND a.phone LIKE CONCAT ('%', #{tb.phone}, '%')
            </if>
            <if test="pageParams.email != null and pageParams.email != ''" >
                AND a.email LIKE CONCAT ('%', #{tb.email}, '%')
            </if>
        </where>

        LIMIT #{start}, #{length}
    </select>

    <select id="count" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM tb_user AS a
        <where>
            <if test="tbUser.username != null and tbUser.username != ''" >
                AND a.username LIKE CONCAT ('%', #{tbUser.username}, '%')
            </if>
            <if test="tbUser.phone != null and tbUser.phone != ''" >
                AND a.phone LIKE CONCAT ('%', #{tbUser.phone}, '%')
            </if>
            <if test="tbUser.email != null and tbUser.email != ''" >
                AND a.email LIKE CONCAT ('%', #{tbUser.email}, '%')
            </if>
        </where>
    </select>

    <insert id="insert">
        INSERT INTO tb_user (
          `username`,
          `password`,
          `phone`,
          `email`,
          `created`,
          `updated`
        )
        VALUES
          (
            #{username},
            #{password},
            #{phone},
            #{email},
            #{created},
            #{updated}
          )
    </insert>

    <delete id="delete">
        DELETE FROM tb_user WHERE id = #{id}
    </delete>

    <delete id="deleteMulti">
        DELETE FROM tb_user
        WHERE user_id IN
        <foreach collection="list" open="(" close=")" separator="," item="id">
            #{id}
        </foreach>
    </delete>

    <update id="update">
        UPDATE
          tb_user
        SET
          username = #{username},
          password = #{password},
          phone = #{phone},
          email = #{email},
          updated = #{updated}
        WHERE id = #{id}
    </update>

    <select id="searchByItem" resultMap="TbUser">
        SELECT
        <include refid="tbUserColumns" />
        FROM tb_user AS a
        <where>
            <if test="username != null and username != ''" >
                AND a.username LIKE CONCAT ('%', #{username}, '%')
            </if>
            <if test="phone != null and phone != ''" >
                AND a.phone LIKE CONCAT ('%', #{phone}, '%')
            </if>
            <if test="email != null and email != ''" >
                AND a.email LIKE CONCAT ('%', #{email}, '%')
            </if>
        </where>
    </select>

    
</mapper>